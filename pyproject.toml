[build-system]
requires = [
    "setuptools>=42",
    "wheel"
]
build-backend = "setuptools.build_meta"

[project]
name = "plasmodesmata_dacapo"
version = "0.1.0"
description = "Tools for working on plasmodesmata project."
authors = [
    { name = "David Ackerman", email = "ackermand@janelia.org" }
]
dependencies = []
[tool.setuptools]
packages = [
  "preprocessing",
  "postprocessing",
]